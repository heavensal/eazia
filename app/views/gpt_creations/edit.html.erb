<%= turbo_frame_tag @gpt_creation do %>
  <%= form_for([@post, @gpt_creation], url: post_gpt_creation_path(@post), method: :patch)  do |f| %>

    <%= f.text_area :description,
      label: false,
      autofocus: true,
      value: @description,
      input_html: { id:"modif-description" ,
                    class:"description",
                    data: {
                      controller: "extends",
                      action: "input->extends#updateTextarea",
                      extends_target: "dimension"
                    }
      }
      %>
    <div class="gpt-edit-bouton">
      <%= link_to "A", post_path(@post), class:"secondary-bouton-annuler" %>
      <%= f.submit "Modifier ✔️", class:"secondary-bouton-modifier" %>
    </div>

  <% end %>
<% end %>
